server:
  port: 8080

spring:
  application:
    name: api-gateway

service:
  identity:
      uri: ${IDENTITY_SERVICE_URI}
  store:
      uri: ${STORE_SERVICE_URI}

jwt:
  publicKey: ${JWT_PUBLIC_KEY}